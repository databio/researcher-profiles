Doe proposes a framework for embedding example region sets into vector spaces so
that downstream machine-learning models can operate on them directly. The method
learns dense representations that preserve co-occurrence structure across region
sets, and it demonstrates that these embeddings improve clustering and retrieval
compared with sparse overlap features.
