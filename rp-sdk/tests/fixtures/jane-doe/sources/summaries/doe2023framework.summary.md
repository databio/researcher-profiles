PlaceholderScape is a provenance metadata framework for preparing example
datasets for machine learning. Doe argues that simple FAIR compliance is
insufficient for AI applications: datasets need deep provenance, explicit
characterization of biases, and documentation of preprocessing decisions. The
framework captures these as structured, machine-readable metadata attached to
each dataset.
