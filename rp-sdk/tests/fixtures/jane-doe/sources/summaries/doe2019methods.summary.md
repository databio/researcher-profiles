This paper presents methods for constructing consensus example region universes.
Rather than treating region-set integration as a preprocessing afterthought, Doe
reframes it as a representational problem with formal solutions involving coverage
cutoffs, maximum-likelihood models, and probabilistic state models. The work
argues that naive union operations discard boundary information and merge distinct
signals, and it benchmarks principled alternatives on synthetic cohorts.
